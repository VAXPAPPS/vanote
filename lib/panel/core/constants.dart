// Core constants
const double minNodeSize = 10;
const double defaultNodeWidth = 220;
const double defaultNodeHeight = 120;
const double gridSize = 30.0;

// Debounce duration for history save
const Duration historySaveDebounce = Duration(milliseconds: 800);
